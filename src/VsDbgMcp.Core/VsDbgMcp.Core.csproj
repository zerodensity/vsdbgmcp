<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <RootNamespace>VsDbgMcp</RootNamespace>
    <AssemblyName>VsDbgMcp.Core</AssemblyName>
  </PropertyGroup>

  <!--
    No package references, on purpose. This assembly is loaded into devenv.exe,
    where every extra dependency competes with Visual Studio's own assembly
    versions. Contracts and plain data only.
  -->

</Project>
